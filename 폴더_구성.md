# v3.8 구현 폴더 구성

## 확정 계획서

- `데이터_전처리_v3.8_전체구현계획.md`: v3.8 독립 구현 기준

## 실행 코드

- `battery_v3_8_preprocessor/`: 전체 전처리 패키지
- `battery_v3_8_preprocessor/README.md`: 설치·dry-run·승인·execute·training-view 설명
- `battery_v3_8_preprocessor/requirements.lock`: 고정 의존성
- `battery_v3_8_preprocessor/pyproject.toml`: 패키지와 CLI 설정
- `battery_v3_8_preprocessor/battery_v3_8/`: Python 모듈
- `battery_v3_8_preprocessor/tests/`: 단위·통합 회귀 테스트

## 보조 실행 파일

- `run_dryrun_keepawake.ps1`: 장시간 dry-run 실행
- `run_execute_keepawake.ps1`: 장시간 execute 실행
- `status_heartbeat.ps1`: 진행 상태 확인
- `prepare_training_view.py`: YOLO 학습 뷰 생성

원본 데이터, EDA 원본 CSV, 생성 데이터셋과 작업 폴더는 저장소에 포함하지 않는다.
